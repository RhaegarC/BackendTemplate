{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Local defaults so a freshly generated project runs without setting env vars.
  // Matches the applicationUrl values in Properties/launchSettings.json.
  // Override with the AllowedOrigins environment variable when needed.
  "AllowedOrigins": "https://localhost:7054,http://localhost:5015"
}
